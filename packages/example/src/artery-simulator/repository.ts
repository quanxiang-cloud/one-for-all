import { Repository } from '@one-for-all/artery-renderer';
import SimulatorInExample from './components';

const simulatorRepo: Repository = {
  'SimulatorInExample@whatever': {
    default: SimulatorInExample,
  },
};

export default simulatorRepo;
