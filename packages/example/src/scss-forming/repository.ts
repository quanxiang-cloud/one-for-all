import { Repository } from '@one-for-all/artery-renderer';
import FormingDemo from './forming-demo';

const repo: Repository = {
  'form-scss@whatever': {
    default: FormingDemo,
  },
};

export default repo;
