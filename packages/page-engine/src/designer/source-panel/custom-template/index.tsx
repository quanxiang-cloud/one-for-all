import React from 'react';

interface Props {
  className?: string;
}

function CustomTemplate(props: Props) {
  return (
    <div>
      custom template
    </div>
  );
}

export default CustomTemplate;
