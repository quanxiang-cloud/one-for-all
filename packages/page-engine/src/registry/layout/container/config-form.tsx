import React from 'react';

interface Props {
  className?: string;
}

function ConfigForm(props: Props): JSX.Element {
  return (
    <div>
      container config
    </div>
  );
}

export default ConfigForm;
