import React from 'react';

interface Props {
  className?: string;
}

function MultiSelect(props: Props) {
  return (
    <div>
      multi select elem
    </div>
  );
}

export default MultiSelect;
