{
  "name": "one-for-all",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run clean",
    "bundle": "lerna run bundle --stream",
    "test": "lerna run test"
  }
}
